<p id="notice"><%= notice %></p>

<p>
  <h1><%= @group.name %> 的寵物</h1>
  <ul>
	<% @group.pets.each do |pet| %>
		<li>
    <%= link_to(pet) do %>
			<strong><%= pet.name %></strong> -- <span>Check it out!</span>
		<% end %>
		</li>
	<% end %>
  </ul>
</p>

<%= link_to 'Edit', edit_group_path(@group) %> |
<%= link_to 'Back', groups_path %>

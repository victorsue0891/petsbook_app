<%= form_for(meal_log) do |f| %>
  <% if meal_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(meal_log.errors.count, "error") %> prohibited this meal_log from being saved:</h2>

      <ul>
      <% meal_log.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pet_id %>
    <%= f.number_field :pet_id %>
  </div>

  <div class="field">
    <%= f.label :food_id %>
    <%= f.number_field :food_id %>
  </div>

  <div class="field">
    <%= f.label :food_amount %>
    <%= f.number_field :food_amount %>
  </div>

  <div class="field">
    <%= f.label :photo_id %>
    <%= f.text_field :photo_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
